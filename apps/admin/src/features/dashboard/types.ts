/**
 * Tipos de períodos pré-configurados suportados pelo dashboard.
 */
export type PeriodPreset = "7d" | "30d" | "90d" | "1y";

/**
 * Modo de seleção de período: pré-configurado (preset) ou customizado por datas.
 */
export type PeriodMode = "preset" | "custom";
